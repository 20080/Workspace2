#include <bits/stdc++.h>

using namespace std;
#define ll long long


int solution() {

	int n, m, d;
	cin >> n >> m >> d;
	vector<vector<int>>arr(n, vector<int>(m, 0));
	vector<int>aaar(n * m, 0);
	int k = 0
	        for (int i = 0; i < n; ++i)
	{
		for (int j = 0; j < m; ++j)
		{
			cin >> aaar[k];
			k++;
		}
	}





	return -1;
}

int main() {
#ifndef ONLINE_JUDGE
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
#endif
	cout << solution() << endl;
	return 0;
}