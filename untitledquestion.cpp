#include <bits/stdc++.h>

using namespace std;
#define ll long long


int main() {
#ifndef ONLINE_JUDGE
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
#endif

	int UL, UR, DR, DL;
	vector<vector<int>>arr(4, vector<int>(4, 0));
	for (int i = 0; i < 4; ++i)
	{
		for (int j = 0; j < 4; ++j)
		{
			cin >> arr[i][j];
		}
	}

	for (int )


		return 0;
}