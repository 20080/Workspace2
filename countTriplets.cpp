#include<bits/stdc++.h>
using namespace std;

#define ll long long



int main() {

#ifndef	ONLINE_JUDGE
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
#endif

	int size;
	int cR;
	cin >> size >> cR;

	vector<int> arr;
	for (int i = 0; i < size; ++i)
	{
		int x;
		cin >> x;
		arr.push_back(x);
	}

	//print subsets//not fucking subsets

	map<int, int> m;

	for (int i = 1; i < ar)ra


		map<int, int>::iterator it;

	for (auto z : m)
	{
		cout << z.first << " " << z.second << endl;
	}





}